<!DOCTYPE html>
<html>
  <head>
    <title>RailsApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css">
  </head>
  <body>
    <% current_user = User.find_by(id: session[:user_id])%>
    <header class="header">
      <h1 class="headerTtl">
        <% if @current_user %>
          <%= link_to "Rails App", posts_index_path %>
        <% else %>
          <%= link_to "Rails App", route_path %>
        <% end %>
      </h1>
      <ul class="headerList">
        <% if current_user %>
          <li class="headerListItem">
            <%= link_to "ホーム", posts_index_path %>
          </li>
          <li class="headerListItem">
            <%= link_to "ユーザー一覧", users_index_path %>
          </li>
          <li class="headerListItem">
            <%= link_to "プロフィール", users_show_path(current_user.id) %>
          </li>
          <li class="headerListItem">
            <%= link_to "投稿する", posts_new_path %>
          </li>
          <li class="headerListItem">
            <%= link_to "ログアウト", logout_path, data: {turbo_method: :post} %>
          </li>
        <% else %>
          <li class="headerListItem">
            <%= link_to "新規登録", signup_path %>
          </li>
          <li class="headerListItem">
            <%= link_to "ログイン", login_path %>
          </li>
        <% end %>
      </ul>
    </header>
    <% if flash[:notice]%>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
